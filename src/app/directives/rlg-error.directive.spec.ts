import { RlgErrorDirective } from './rlg-error.directive';

describe('RlgErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new RlgErrorDirective();
    expect(directive).toBeTruthy();
  });
});
